@model IEnumerable<Hotel_reservas.Models.Tipo_pago>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.tipo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.tipo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_tipo_pago }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_tipo_pago }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_tipo_pago })
        </td>
    </tr>
}

</table>
